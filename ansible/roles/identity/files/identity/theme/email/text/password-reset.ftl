${msg("passwordResetSubject")}

${msg("passwordResetBodyHtml",user.firstName!'')}

${msg("passwordResetBody1")}

${link}

${msg("passwordResetBody2",linkExpiration)}

${msg("passwordResetDisclaimer")}

© ${.now?string('yyyy')} ${realmName}. All rights reserved.
