server {
  listen        10080 default_server;
  server_name   _;

  location /health {
      access_log off;
      return 200 "healthy\n";
  }
  
  location / {
      return 301 https://$host$request_uri;
  }  

}